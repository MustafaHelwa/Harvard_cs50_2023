#include <stdio.h>
#include <cs50.h>

int main (void)
{
    int start;
    do
    {
        start = get_int ("Starting size: ");

    }
    while (start <9);

    int end;
    do
    {
        end = get_int ("Ending size: ");
    }
}