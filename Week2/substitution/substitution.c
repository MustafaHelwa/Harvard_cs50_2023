#include <cs50.h>
#include <string.h>
#include <stdio.h>

string keyChecker (string xVal);

int main (int argc, string argv[])
{
    key = keyChecker (argv[]);
}

string keyChecker (string xVal)
{
    if (strlen(sxVal) < 26)
    {
        printf("Key must contain 26 characters.");
        return 1;
    }


    for (i < argc, )
}